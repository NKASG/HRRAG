# HRRAG

Orange-HR assistant with:
- WhatsApp webhook endpoint (`/webhook`)
- New browser-based web chat view (`/`)

## Run locally

```bash
python app.py
```

Open `http://localhost:5000` for the web chat UI.
